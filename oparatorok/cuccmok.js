

let diak = 20;

// diak = diak - 1;

// diak = diak - 1;

// diak = diak * 2;

// diak = diak / 2;

let extra_diakok = diak % 3;


diak -= 1 // lehet röviditeni mint pythonban 

console.log(diak);
console.log(extra_diakok);


muveleti_sorrend = 3 + 3 - 6 * (64**0.5) // müveleti sorrend ugyanugy megvan itt is mint a matekban 

console.log(muveleti_sorrend)